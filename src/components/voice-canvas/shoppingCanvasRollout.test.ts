import{describe,expect,it}from"vitest";import{isShoppingCanvasEnabled,parseShoppingCanvasRolloutConfig}from"./shoppingCanvasRollout";describe("shopping Canvas rollout",()=>{it("fails closed",()=>expect(parseShoppingCanvasRolloutConfig({enabled:true,rolloutPercent:"bad"})).toEqual({enabled:true,rolloutPercent:0}));it("supports immediate full enable and disable",()=>{expect(isShoppingCanvasEnabled({enabled:true,rolloutPercent:100},"user")).toBe(true);expect(isShoppingCanvasEnabled({enabled:false,rolloutPercent:100},"user")).toBe(false)})});
