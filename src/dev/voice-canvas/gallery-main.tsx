import React from "react";
import ReactDOM from "react-dom/client";
import VoiceCanvasGallery from "./gallery";

ReactDOM.createRoot(document.getElementById("root")!).render(<React.StrictMode><VoiceCanvasGallery /></React.StrictMode>);
